name: Deploy Staging

on:
  push:
    branches:
      - main
    paths:
      - '**/*.vue'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get the PR that triggered the push
        run: echo "$GITHUB_EVENT_PATH"
